# 🏢 Автоматический Агрегатор Недвижимости для Софии

Полное руководство по настройке и использованию автоматического агрегатора объявлений о недвижимости от всех ведущих агентств Софии, Болгария.

## 📋 Содержание

1. [Быстрый старт](#быстрый-старт)
2. [Настройка агентств](#настройка-агентств)
3. [Автоматический мониторинг](#автоматический-мониторинг)
4. [Расширенные возможности](#расширенные-возможности)
5. [Примеры использования](#примеры-использования)

---

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Инициализация базы данных

```bash
npm run db:init
```

### 3. Быстрая настройка агрегатора

```bash
# Автоматическая настройка
npm run auto:setup

# Или настройте вручную
npm run auto:config -- --min-price 500 --max-price 2000 --enable
```

### 4. Запуск агрегатора

```bash
# Однократный запуск (тестирование)
npm run auto:once

# Непрерывный мониторинг
npm run auto:start
```

---

## 🏢 Настройка агентств

### Просмотр настроенных агентств

```bash
npm run aggregator:list
```

Покажет все настроенные агентства недвижимости с их статусом.

### Настроенные агентства (по умолчанию)

1. **UES.bg** - Unique Estates ✅
2. **Luximmo.bg** - Luximmo Finest Estates ✅
3. **BulgarianProperties.com** - Bulgarian Properties ✅
4. **Yavlena.com** - Yavlena (Явлена) ✅
5. **Address.bg** - АДРЕС Недвижими имоти ✅

### Добавление новых агентств

#### Автоматический анализ сайта

```bash
# Анализ сайта агентства
npm run aggregator:analyze https://example-agency.bg --name "Agency Name" --save

# С AI валидацией (рекомендуется!)
npm run aggregator:analyze https://example-agency.bg --name "Agency Name" --ai --save

# Автоматическая настройка всех приоритетных агентств
npm run aggregator:setup

# С AI валидацией
npm run aggregator:setup -- --ai
```

#### 🤖 AI Валидация (Новинка!)

AI автоматически проверяет качество найденных конфигураций:

**Что проверяется:**
- ✅ Надежность CSS селекторов
- ✅ Корректность извлеченных данных
- ✅ Реалистичность цен для рынка Софии
- ✅ Полнота данных

**Настройка:**

```bash
# 1. Получите бесплатный API ключ
# Google Gemini: https://makersuite.google.com/app/apikey
# или OpenAI: https://platform.openai.com/api-keys

# 2. Настройте .env
cp .env.example .env
nano .env
# Добавьте: GEMINI_API_KEY=ваш_ключ

# 3. Используйте с флагом --ai
npm run aggregator:analyze https://site.bg --name "Site" --ai
```

**Подробнее:** [AI_VALIDATION_GUIDE.md](./AI_VALIDATION_GUIDE.md)

#### Включение/отключение агентств

```bash
# Отключить агентство
npm run aggregator toggle ues_bg

# Включить обратно
npm run aggregator toggle ues_bg
```

---

## 🔄 Автоматический мониторинг

### Настройка фильтров

```bash
# Установить диапазон цен (в EUR)
npm run auto:config -- --min-price 800 --max-price 1500

# Настроить email для уведомлений
npm run auto:config -- --email your-email@example.com

# Установить интервал проверки (каждые 10 минут)
npm run auto:config -- --interval "*/10 * * * *"
```

### Просмотр текущей конфигурации

```bash
npm run auto:config -- --show
```

Выведет:
- Статус мониторинга (включен/выключен)
- Интервал проверки
- Фильтры цен
- Список отслеживаемых городов
- Настройки уведомлений

### Управление мониторингом

```bash
# Запустить мониторинг
npm run auto:start

# Однократная проверка (для тестирования)
npm run auto:once

# Проверить статус
npm run auto:status

# Включить/отключить
npm run auto:config -- --enable
npm run auto:config -- --disable
```

### Расписание (Cron expressions)

```bash
# Каждые 5 минут
npm run auto:config -- --interval "*/5 * * * *"

# Каждые 15 минут (рекомендуется)
npm run auto:config -- --interval "*/15 * * * *"

# Каждый час
npm run auto:config -- --interval "0 * * * *"

# Каждые 30 минут в рабочее время (9-18)
npm run auto:config -- --interval "*/30 9-18 * * *"

# Каждый час в будни
npm run auto:config -- --interval "0 9-18 * * 1-5"
```

---

## 📊 Статистика и отчеты

### Просмотр статистики агрегатора

```bash
npm run aggregator:stats
```

Выводит:
- Общее количество собранных объявлений
- Количество новых объявлений за последние 24 часа
- Распределение по источникам (агентствам)
- Статистику сессий сбора данных

### Веб-интерфейс для анализа

```bash
npm run web
```

Откройте http://localhost:3000 для:
- 📊 Интерактивных графиков и трендов
- 🔍 Расширенного поиска с фильтрами
- 📈 Анализа рынка и цен
- 💾 Сохранения избранных объявлений

---

## 🔧 Расширенные возможности

### Ручной запуск сбора данных

```bash
# Собрать данные со всех включенных агентств
npm run aggregator:run

# Собрать данные с конкретного агентства
npm run aggregator:run -- --site ues_bg

# Тестовый режим (ограниченные результаты)
npm run aggregator:run -- --test
```

### Работа с базой данных

```bash
# Проверить статус БД
npm run db:status

# Посмотреть статистику
npm run stats

# Полное сканирование (999 страниц)
npm run scan:full

# Быстрое сканирование (5 страниц)
npm run scan:db
```

### Экспорт конфигурации

```bash
# Экспортировать конфигурацию всех агентств
npm run aggregator export -o my-config.json
```

---

## 💡 Примеры использования

### Пример 1: Поиск бюджетной квартиры

```bash
# Настроить фильтры
npm run auto:config -- --min-price 400 --max-price 800

# Запустить частые проверки
npm run auto:config -- --interval "*/5 * * * *"

# Начать мониторинг
npm run auto:start
```

### Пример 2: Мониторинг премиум-сегмента

```bash
# Премиум фильтры
npm run auto:config -- --min-price 2000 --max-price 5000

# Проверка раз в час
npm run auto:config -- --interval "0 * * * *"

# Запуск
npm run auto:start
```

### Пример 3: Быстрый тест перед настройкой

```bash
# Однократная проверка всех источников
npm run auto:once

# Посмотреть что нашлось
npm run web
```

### Пример 4: Добавление нового агентства

```bash
# Проанализировать новый сайт
npm run aggregator:analyze https://new-agency.bg \
  --name "New Agency" \
  --save

# Проверить что добавилось
npm run aggregator:list

# Протестировать новое агентство
npm run aggregator:run -- --site new-agency_bg --test
```

---

## 📧 Настройка уведомлений

### Email уведомления

```bash
# Настроить email (используйте существующую систему)
npm run auto:config -- --email your-email@gmail.com

# Для Gmail: используйте App Password
# 1. Включите 2FA: https://myaccount.google.com/security
# 2. Создайте App Password: https://myaccount.google.com/apppasswords
# 3. Используйте в monitor:setup
```

Подробнее см. [MONITORING_SETUP.md](./MONITORING_SETUP.md)

---

## 🎯 Лучшие практики

### 1. Частота мониторинга

- **Активный поиск**: каждые 5-10 минут
- **Обычный режим**: каждые 15-30 минут
- **Фоновый мониторинг**: каждый час

### 2. Фильтры цен

- Устанавливайте реалистичный диапазон
- Слишком узкие фильтры могут пропустить хорошие варианты
- Начните шире, потом сужайте

### 3. Управление источниками

```bash
# Отключите медленные или нестабильные источники
npm run aggregator toggle slow_agency_id

# Периодически проверяйте статус
npm run aggregator:list
```

### 4. Мониторинг производительности

```bash
# Регулярно проверяйте статистику
npm run aggregator:stats

# Используйте веб-интерфейс для анализа
npm run web
```

---

## 🐛 Устранение проблем

### Нет новых объявлений

```bash
# Проверьте статус агентств
npm run aggregator:list

# Проверьте фильтры
npm run auto:config -- --show

# Запустите тестовый сбор
npm run auto:once
```

### Ошибки при сборе данных

```bash
# Проверьте логи последней сессии
npm run aggregator:stats

# Протестируйте проблемное агентство
npm run aggregator:run -- --site agency_id --test

# Отключите проблемное агентство временно
npm run aggregator toggle agency_id
```

### База данных

```bash
# Проверьте подключение
npm run db:status

# Переинициализируйте при необходимости
npm run db:init
```

---

## 📚 Дополнительные ресурсы

- [Настройка мониторинга](./MONITORING_SETUP.md) - подробное руководство по уведомлениям
- [Основной README](./README.md) - общая информация о проекте
- [API документация](./api_info_example.md) - работа с API

---

## 🎉 Готово к использованию!

Теперь у вас есть полноценный автоматический агрегатор недвижимости для Софии!

### Быстрый чеклист запуска:

- [ ] Установлены зависимости (`npm install`)
- [ ] Инициализирована БД (`npm run db:init`)
- [ ] Настроены фильтры (`npm run auto:config`)
- [ ] Настроены email уведомления (опционально)
- [ ] Проверены агентства (`npm run aggregator:list`)
- [ ] Запущен тестовый сбор (`npm run auto:once`)
- [ ] Запущен автоматический мониторинг (`npm run auto:start`)

**Готово!** 🎊 Система будет автоматически собирать новые объявления и уведомлять вас о них.

---

## 🆘 Поддержка

Если возникли вопросы:
1. Проверьте раздел [Устранение проблем](#устранение-проблем)
2. Посмотрите статистику: `npm run aggregator:stats`
3. Проверьте веб-интерфейс: `npm run web`
